import 'package:flutter/material.dart';

class AppColors {
  // Nền & khung
  static const scaffold = Color(0xFFF7F7FB);
  static const border   = Color(0xFFE5E7EB);
  static const surfaceGray = Color(0xFFF1F3F7);

  // Văn bản
  static const text       = Color(0xFF111827);
  static const textMuted  = Color(0xFF6B7280);
  static const textSubtle = Color(0xFF9CA3AF);

  // Chủ đạo & action
  static const primary = Color(0xFF0B0F1A);

  // Màu nhấn (đang dùng trong Quick Actions & Stats)
  static const accentBlue   = Color(0xFF2563EB);
  static const accentGreen  = Color(0xFF22C55E);
  static const accentPurple = Color(0xFFA855F7);
  static const accentOrange = Color(0xFFF97316);
  static const accentYellow = Color(0xFFF59E0B);
  static const accentGreenDeep = Color(0xFF16A34A);

  // Nền icon/ảnh đại diện
  static const iconBgBlue = Color(0xFFE8F0FF);
}
